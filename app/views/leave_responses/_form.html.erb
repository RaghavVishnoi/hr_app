<%= form_for(leave_response) do |f| %>
  <% if leave_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(leave_response.errors.count, "error") %> prohibited this leave_response from being saved:</h2>

      <ul>
      <% leave_response.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
